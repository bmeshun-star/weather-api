services:
  weather-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"

  alert-service:
    build: ./alert-service
    ports:
      - "8001:8001"
    depends_on:
      - weather-service
